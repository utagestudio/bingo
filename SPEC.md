# OBS向けWebビンゴツール 仕様設計

## 目的

OBSのウィンドウキャプチャで取り込んで表示できる、配信向けのWebビンゴ盤面ツールを作る。ブラウザだけで完結し、入力項目、盤面レイアウト、保存スロットをLocalStorageへ自動保存して、リロード後も同じ状態を復元する。

## 想定ユーザーと利用環境

- 配信者または配信スタッフが、事前にビンゴ項目を入力して盤面を作る。
- OBSでは通常ブラウザで開いたツール画面をウィンドウキャプチャで取り込む。
- アドレスバーやブラウザUIはOBS側のクロップで除外する。
- OBSブラウザソースでの読み込みは副運用とし、LocalStorageが通常ブラウザと共有されない場合は空状態になる点に注意する。
- 操作画面と表示画面は同じアプリ内で切り替えられるようにする。
- デプロイ先はCloudflareを想定する。
- サーバー側DBやログインは初期スコープに含めない。

## 技術方針

### 初期実装

- フロントエンドのみのSPAとして実装する。
- 状態保存はLocalStorageを使う。
- ランダム並び替え、ドラッグアンドドロップ、盤面切り替えはクライアント内で完結させる。
- データを外部に送信しないため、配信用ツールとして扱いやすい。

### 推奨スタック

- Vite + TypeScript
- React
- CSS Modulesまたは通常のCSS
- ドラッグアンドドロップは `@dnd-kit` を優先候補にする。

理由:

- Cloudflare Workers Static AssetsまたはCloudflare Pagesへ静的ビルドを配信しやすい。
- 将来、共有URL、クラウド保存、API連携を追加する場合もWorkersへ拡張しやすい。
- Reactで編集状態、スロット状態、ドラッグ操作を扱いやすい。

### React実装方針

実装時は、できる限り役割ごとに細かくファイルを分ける。

- UIコンポーネント、状態管理、盤面生成ロジック、LocalStorage処理、多言語辞書、型定義を分離する。
- 盤面サイズ計算、Free Space配置、リーチ/ビンゴ判定などの純粋ロジックはReactコンポーネントから切り離す。
- Reactコンポーネントは表示とユーザー操作の橋渡しに集中させる。
- コードの意図が読み取りにくい箇所には、日本語で簡潔なコメントを追加する。
- コメントは処理の直訳ではなく、「なぜその処理が必要か」「どの仕様を守るためか」を説明する。

## デプロイ方針

Cloudflare Workers Static Assetsを第一候補にする。ビルド成果物の `dist` を静的アセットとして配信し、必要に応じてWrangler設定にSPAフォールバックを指定する。

Cloudflareの現行ドキュメントでは、Workersは静的アセットをWorkerと一緒に配信でき、SPAでは存在しないパスを `index.html` に返す `not_found_handling = "single-page-application"` が使える。静的アセットへのリクエストはWorkerを起動せず配信できる。

参考:

- https://developers.cloudflare.com/workers/static-assets/
- https://developers.cloudflare.com/workers/static-assets/binding/
- https://developers.cloudflare.com/workers/static-assets/billing-and-limitations/

## 主要機能

### 1. 項目入力

ユーザーはビンゴ項目を複数行テキストとして入力する。

- 1行を1項目として扱う。
- 空行は無視する。
- 前後の空白は保存時にtrimする。
- 重複項目は許可する。
- 入力内容は変更のたびにLocalStorageへ自動保存する。

初期スコープでは項目ごとの詳細設定、画像、色、タグは扱わない。

### 2. 自動盤面サイズ

入力された項目数に応じて、正方形の盤面サイズを自動決定する。

計算:

```text
itemCount = 有効な入力項目数
boardSize = max(1, ceil(sqrt(itemCount)))
cellCount = boardSize * boardSize
freeSpaceCount = cellCount - itemCount
```

例:

| 項目数 | 盤面 |
| ---: | --- |
| 1 | 1 x 1 |
| 2-4 | 2 x 2 |
| 5-9 | 3 x 3 |
| 10-16 | 4 x 4 |
| 17-25 | 5 x 5 |

上限は初期実装では明示的に設けないが、OBS表示と操作性を考え、UI上は25項目程度までを推奨表示する。大量項目でも破綻しないよう、セルの最小文字サイズと折り返しを設計する。

### 3. Free Space自動配置

盤面の不足分は自動で `Free Space` として埋める。

Free Spaceはできるだけ中央に寄せる。複数ある場合は、中央セルからの距離が近い順に配置する。

配置アルゴリズム:

1. `boardSize x boardSize` の全セル座標を作る。
2. 各セルについて、盤面中心からの距離を計算する。
   - 中心座標は `((boardSize - 1) / 2, (boardSize - 1) / 2)`。
   - 距離は二乗距離 `dx * dx + dy * dy` を使う。
3. 距離が近い順、同距離なら行、列の順でソートする。
4. `freeSpaceCount` 個のセルをFree Spaceにする。
5. 残りのセルへ項目を順番に配置する。

例:

- 3 x 3で項目8個の場合、中央1マスがFree Space。
- 4 x 4で項目15個の場合、中央に近い4マスのうち左上寄りの1マスがFree Space。
- 4 x 4で項目12個の場合、中央4マスがFree Space。

### 4. ランダム並び替え

現在の項目をランダムに並び替えて盤面へ反映できる。

- 編集モード中のみ実行可能。
- Free Spaceセルの位置は原則として自動配置ルールに従う。
- ランダム化対象は入力項目のみ。
- Free Spaceセルは移動せず、中央優先の位置を維持する。
- Fisher-Yates shuffleを使う。
- 実行後の並び順はLocalStorageへ保存する。

### 5. ドラッグアンドドロップ並び替え

盤面上のセルをドラッグして項目の位置を変更できる。

- 編集モード中のみ有効。
- 使用モードまたは表示モードではドラッグ操作を無効化する。
- Free Spaceセルはドラッグ不可、ドロップ先にもできない。
- D&Dで移動できるのは項目セル同士のみ。
- 移動後の盤面配列をLocalStorageへ保存する。
- キーボード操作への配慮として、将来的に選択中セルの矢印移動や入れ替えにも対応できる設計にする。

### 6. 編集モード

誤操作を避けるため、盤面編集操作は編集モードでのみ可能にする。

編集モードON:

- 項目入力欄を表示する。
- ランダム並び替えボタンを有効化する。
- ドラッグアンドドロップを有効化する。
- 保存スロット名の編集を許可する。

編集モードOFF:

- 盤面表示を主役にする。
- 入力欄や編集系ボタンは非表示、またはOBSで邪魔にならない位置へ折りたたむ。
- ドラッグアンドドロップを無効化する。
- セルクリックまたはタップによる項目開けを有効化する。
- 盤面切り替えボタンだけは表示してよい。

OBSのウィンドウキャプチャ用には、編集モードOFFを実質的な配信表示モードとして扱う。

編集モードOFF時:

- 操作パネルを非表示にする。
- 盤面を画面中央に大きく表示する。
- 背景はツール側の背景をそのまま表示する。
- OBS側でクロップしやすいよう、盤面領域の位置と余白を安定させる。
- 編集モードへ戻るための小さな復帰ボタンを用意する。
- 補助操作としてキーボードショートカットでも編集モードへ戻れるようにしてよい。

OBSブラウザソースなど、別ブラウザコンテキストで表示専用URLを使いたい場合に備え、URLクエリでoverlay表示もできるようにする。

```text
/?view=overlay
```

overlay表示では編集UIを出さない。ただし初期スコープの保存方式はLocalStorageなので、通常ブラウザで編集した状態がOBSブラウザソース側へ共有されるとは限らない。初期運用ではウィンドウキャプチャを推奨する。

### 7. 最大3盤面保存

盤面は最大3つまで保存できる。

- スロットは `Board 1`, `Board 2`, `Board 3`。
- それぞれ独立した項目、並び順、盤面サイズ、表示設定を持つ。
- ボタン一つでアクティブ盤面を切り替える。
- アクティブスロットIDもLocalStorageへ保存する。
- スロット切り替え時は即座に盤面表示へ反映する。
- Board 1の初期状態には、ゲーム実況向けのサンプルとしてマインクラフト用5 x 5ビンゴを入れる。
- Board 1の初期タイトルは、言語に応じて `マインクラフトビンゴ（サンプル）` または `Minecraft Bingo (Sample)` とする。
- Board 2、Board 3は空の盤面として開始する。

初期実装では「保存」ボタンを必須にせず、自動保存を基本にする。ただしユーザーが保存済みであることを理解しやすいよう、最終保存時刻や `保存済み` 表示を置く。

### 8. OBS表示

OBSのウィンドウキャプチャで扱いやすい表示にする。

要件:

- 背景はツール側の背景をそのまま配信に載せる。
- 16:9表示に収まりやすい。
- ブラウザウィンドウのリサイズに追従する。
- 文字がセルからはみ出しにくい。
- 編集モードOFFでは操作UIを非表示にできる。
- OBS側でクロップしやすいよう、盤面の位置と外側余白を安定させる。
- CSSで背景色、文字色、セル色を変えやすい。

推奨表示サイズ:

- 1920 x 1080
- 1280 x 720

CSS方針:

- 盤面は `display: grid` を使う。
- `grid-template-columns: repeat(boardSize, 1fr)` を動的に設定する。
- セルは固定アスペクト比に近づける。
- 長文は折り返し、必要に応じてフォントサイズを下げる。
- 盤面サイズごとにフォントサイズを段階指定し、最大行数を制限する。
- フルスクリーン表示やOBSクロップ時に盤面が大きく安定して映るよう、表示モードの余白を一定にする。

### 9. Bingo状態

初期スコープで、項目開け、リーチ表示、ビンゴ表示に対応する。

項目開け:

- セルをクリックまたはタップすると `marked` を切り替える。
- 項目開けは使用モードまたは表示モードでのみ有効にする。
- 編集モードではセルクリックによる `marked` 切り替えを行わない。
- `marked: true` のセルはハイライト表示にする。
- Free Spaceは初期状態で `marked: true` として扱う。
- Free Spaceセルは常に開いた扱いとし、クリックやタップで閉じられない。
- overlay画面でもセルの開閉は可能にする。ただし誤操作が気になる場合に備え、将来 `?interactive=0` で開閉不可にできる余地を残す。

リーチ表示:

- 各行、列、対角線をビンゴライン候補として扱う。
- 未開封セルが1つだけ残っているラインをリーチと判定する。
- リーチ中のラインは控えめなハイライト、または対象未開封セルのアクセント表示で示す。
- 複数リーチが同時に成立する場合はすべて表示する。

ビンゴ表示:

- すべてのセルが `marked: true` のラインをビンゴと判定する。
- ビンゴ成立ラインはリーチより強いハイライトで表示する。
- 複数ラインが成立する場合はすべて表示する。
- ビンゴ成立時の派手なアニメーションはOBS上で邪魔にならない控えめなものにする。
- セル状態の表示優先順位は `bingo > reach > marked > normal` とする。

判定ルール:

```text
lines = rows + columns + diagonals
bingoLines = lines where every cell.marked is true
reachLines = lines where marked count is boardSize - 1
             and unmarked count is 1
             and line is not already bingo
```

盤面サイズが1 x 1の場合、Free Spaceのみなら即ビンゴと判定される。

### 10. 多言語対応

初期スコープで日本語と英語に対応する。

- 対応言語は `ja` と `en`。
- UI文言は辞書ファイルまたは辞書オブジェクトで管理する。
- ユーザーが入力したビンゴ項目名は翻訳しない。
- Free Spaceの表示文言は言語に応じて切り替える。
  - 日本語: `Free Space`
  - 英語: `Free Space`
- 言語設定はLocalStorageへ保存する。
- 初回表示は `navigator.language` が `ja` で始まる場合は日本語、それ以外は英語にする。
- URLクエリで言語を上書きできるようにする。
- 初期サンプル盤面や初期状態リセットで投入するサンプル項目は、現在のUI言語に応じて日本語または英語を選ぶ。
- ユーザーが入力した項目は翻訳しないが、システムが用意する初期サンプルはローカライズ対象とする。

```text
/?lang=ja
/?lang=en
```

将来候補:

- 抽選履歴。
- 盤面のPNG書き出し。

## データ設計

### LocalStorageキー

```text
obs-bingo-tool:v1
```

### 保存データ

```ts
type AppState = {
  version: 1;
  activeBoardId: "board-1" | "board-2" | "board-3";
  editMode: boolean;
  locale: "ja" | "en";
  boards: Record<BoardId, BoardState>;
};

type BoardId = "board-1" | "board-2" | "board-3";

type BoardState = {
  id: BoardId;
  name: string;
  rawInput: string;
  items: BingoItem[];
  layout: BingoCell[];
  updatedAt: string;
  appearance: BoardAppearance;
};

type BingoItem = {
  id: string;
  label: string;
};

type BingoCell = {
  id: string;
  type: "item" | "free";
  itemId?: string;
  label: string;
  marked: boolean;
};

type LineStatus = {
  kind: "row" | "column" | "diagonal";
  index: number;
  cellIds: string[];
  status: "none" | "reach" | "bingo";
};

type BoardAppearance = {
  transparentBackground: boolean;
  theme: "light" | "dark";
  displayScale: "compact" | "standard" | "fit";
};
```

### 初期サンプル盤面

Board 1は、通常の数字ビンゴではなく、ゲーム実況で課題達成時に項目を開ける用途を示すサンプル盤面として開始する。

- 日本語初期タイトル: `マインクラフトビンゴ（サンプル）`
- 英語初期タイトル: `Minecraft Bingo (Sample)`
- 初期項目数は24項目とし、5 x 5盤面の中央がFree Spaceになるようにする。
- 現在の言語が日本語なら日本語のマインクラフト項目、英語なら英語のマインクラフト項目を投入する。
- `初期状態に戻す` を実行した場合も、同じ言語ルールで現在のスロットを初期化する。
- 既存ユーザーのLocalStorageに保存済みデータがある場合は、自動で上書きせず、明示的なリセット操作で初期サンプルを再投入する。

### 復元ルール

- LocalStorageに有効なデータがあれば復元する。
- JSON parseに失敗した場合は初期状態へ戻す。
- `version` が異なる場合はマイグレーション関数を通す。
- `layout` と `items` に不整合がある場合は、`rawInput` から盤面を再生成する。

### 自動保存タイミング

- 入力変更後、短いdebounceを挟んで保存する。
- ランダム並び替え直後に保存する。
- ドラッグ完了直後に保存する。
- セルの `marked` 変更直後に保存する。
- スロット切り替え、テーマ変更、編集モード変更時に保存する。
- 言語変更時に保存する。

## 盤面生成仕様

### 入力から項目作成

```text
rawInput
  -> split by newline
  -> trim each line
  -> remove empty lines
  -> map to BingoItem
```

IDは安定性を優先する。

- 入力行の順番を保持した `BingoItem[]` を作る。
- 既存の同じ行位置に項目があり、trim後のラベルが同じ場合は既存IDを再利用する。
- 同じ行位置のラベルが変わった場合は、同じIDを維持してラベルだけ更新する。
- 行が追加された場合は新規IDを発行する。
- 行が削除された場合は、その行に対応する項目IDを削除対象にする。
- 重複ラベルは許可し、IDで別項目として扱う。
- 新規項目には `crypto.randomUUID()` を使う。
- `crypto.randomUUID()` が使えない環境では `Date.now()` と乱数で代替する。

### レイアウト再生成

入力項目が増減した場合、盤面サイズとFree Space数を再計算する。

- 既存項目IDが維持される限り、セル上の並び順と `marked` 状態を保持する。
- ラベルだけが変更された項目は、同じセル位置でラベルを更新する。
- 削除された項目IDを含むセルは取り除く。
- 追加された項目IDは、既存項目セルの後ろに入力順で追加する。
- 盤面サイズが変わらない場合、既存項目セルの相対順を保持し、Free Spaceだけを中央優先位置へ再配置する。
- 盤面サイズが変わる場合、既存項目セルの現在順を保持したまま新サイズへ再配置し、Free Spaceは中央優先位置へ配置する。
- Free Spaceはユーザー操作で移動できないため、入力編集やランダム並び替え後も常に中央優先位置へ戻す。
- Free Spaceは生成または再生成時に `marked: true` とする。

このルールにより、入力編集とドラッグ並び替えが衝突しにくくなる。

## 画面設計

### 通常画面

- 上部: スロット切り替え、テーマ、言語、透過設定、表示サイズ、表示モード切り替え
- 中央: ビンゴ盤面
- 下部またはサイド: 項目入力欄、スロット名、ランダム並び替え、補助操作
- `テーマ`、`言語`、`透過` は表示や環境に関する設定として左側にまとめる。
- `表示サイズ` と `表示モード` はOBS取り込み時の見え方に関する操作として、並び順を `表示サイズ`、`表示モード` とし、右寄せのグループにする。
- ランダム並び替えは入力された項目に対する操作なので、項目入力エリアの近くに配置する。
- ランダム並び替えボタンの日本語ラベルは `ビンゴのセルの位置をシャッフルする` とする。
- 編集モードではフッターに著作権リンクとフィードバックリンクを表示する。
- 右パネルの空き領域には、表示モードへ切り替えるとビンゴとして使えること、編集モードではドラッグでセル位置を移動できることを簡潔に説明する。

## デザイン方針

### 全体方針

配信画面に載せても読みやすく、編集時には迷わず操作できる実用的なデザインにする。装飾性よりも視認性、状態の分かりやすさ、OBSでの取り込みやすさを優先する。

- 表示モードは盤面を主役にする。
- 編集モードは作業画面として、入力、並び替え、切り替えが分かりやすい構成にする。
- 配信画面で目立ちすぎる装飾や過度なアニメーションは避ける。
- 盤面、セル、状態表示のコントラストを高く保つ。
- 画面サイズが変わっても、盤面の位置と余白が大きく変わらないようにする。

### ビジュアルトーン

初期テーマは `light` とし、配信画面に明るく載せやすいライト寄りのデザインを基本にする。白背景一色で平板にならないよう、淡い背景、明確なセル境界、読みやすい文字色で構成する。

- 背景: 明るい無地または淡い色面
- 通常セル: 白またはごく淡い面と濃い文字で明確に読む
- Free Space: 通常セルと少し違う淡い色で区別する
- markedセル: 開いたことが一目で分かる強めのハイライト
- reachライン: 注意を引くが主張しすぎないアクセント
- bingoライン: 最も強い成功状態として、色、枠、軽い発光などで明確に示す

`dark` テーマも初期スコープで提供する。暗いゲーム画面や配信レイアウトに合わせたい場合に、背景、セル、文字、枠線を暗色系へ切り替えられるようにする。

- テーマは盤面ごとに保存する。
- テーマ切り替えは編集モード中に操作できる。
- 表示モードとoverlayにも即時反映する。
- marked、reach、bingoの意味色はテーマ間で大きく変えず、背景とのコントラストだけ調整する。
- ユーザーが入力した項目やmarked状態はテーマ切り替えで変化させない。

lightテーマのカラートークン例:

```text
background: #f8fafc
surface: #ffffff
cell: #ffffff
cellText: #111827
freeCell: #e0f2fe
marked: #fde68a
reach: #38bdf8
bingo: #22c55e
danger: #ef4444
mutedText: #64748b
border: #cbd5e1
```

darkテーマのカラートークン例:

```text
background: #111827
surface: #1f2937
cell: #0f172a
cellText: #f8fafc
freeCell: #1e3a8a
marked: #facc15
reach: #38bdf8
bingo: #22c55e
danger: #f87171
mutedText: #cbd5e1
border: #475569
```

### 盤面デザイン

- 盤面は正方形に近い比率で表示する。
- セル間のgapはOBS上でも罫線として見える程度にする。
- セル角丸は控えめにする。
- 長い項目名は折り返して中央揃えにする。
- 文字サイズは盤面サイズに応じて段階的に調整する。
- セル内テキストがはみ出す場合は、折り返し、行数制限、フォント縮小の順で対応する。
- ビンゴラインの表示は、セル全体の色変化とライン状の強調を組み合わせてもよい。

フォント段階の初期値:

| 盤面サイズ | セル文字サイズ | 最大行数 |
| ---: | ---: | ---: |
| 1 x 1 | 48px | 3 |
| 2 x 2 | 38px | 3 |
| 3 x 3 | 30px | 3 |
| 4 x 4 | 24px | 3 |
| 5 x 5 | 20px | 3 |
| 6 x 6以上 | 16px | 2 |

実装時は `clamp()` を併用し、画面幅が狭い場合もセル内に収まりやすくする。

### 状態表現

状態は色だけに依存しない。

- marked: 背景色変更、チェック風アイコン、または軽い影で表現する。
- reach: 該当ラインまたは未開封セルにアクセント枠を出す。
- bingo: 該当ライン全体を強調し、必要なら短いアニメーションを1回だけ再生する。
- ドラッグ中: 持ち上げ状態、ドロップ先、入れ替え候補が分かるようにする。
- 編集モードON: 画面全体のどこかで編集状態が分かる表示にする。
- 複数状態が重なる場合は `bingo > reach > marked > normal` の優先順位で見た目を決める。

### 編集画面デザイン

編集モードは操作効率を優先する。

- 盤面プレビューを常に見える位置に置く。
- 項目入力欄は複数行テキストとして十分な高さを確保する。
- スロット切り替えは3つのセグメントボタンにする。
- 編集モード切り替えはトグルとして明確にする。
- ランダム並び替えは誤操作を避けるため、編集モード中だけ有効化する。
- ランダム並び替えボタンは、項目入力欄の近くに置き、入力項目の並びを盤面上でシャッフルする操作であることを明確にする。
- 自動保存状態は控えめに表示する。
- 言語切り替え、テーマ設定は主操作より控えめに置く。
- 右パネルには、初回利用時に迷いやすい表示モード切り替えとドラッグ並び替えについて、短い説明書きを置く。
- 右側の編集パネル下部には、破壊的または一括変更系の補助操作をまとめる。
- `選択状態を全解除する` は `初期状態に戻す` の上に置き、項目セルのmarked状態だけを解除する。
- `初期状態に戻す` は現在のスロットを初期化する操作なので、目立ちすぎない控えめなボタン表現にする。
- `初期状態に戻す` はFree Space以外のmarked状態、入力項目、盤面名、並び順を現在言語の初期状態へ戻す。

### 表示モードデザイン

表示モードはOBSウィンドウキャプチャの主運用画面として扱う。

- 盤面を中央に大きく表示する。
- 表示サイズは `compact`、`standard`、`fit` を切り替えられるようにする。
- `compact` はOBSでクロップしやすい小さめ表示、`standard` は編集モードに近い大きめ表示とする。
- `fit` はブラウザ内で取れる正方形領域にできるだけ合わせる表示とする。
- 表示サイズは盤面ごとに保存する。
- 編集用UIは表示しない。
- 背景は配信に載る前提で、見切れても違和感がない余白を持たせる。
- 編集復帰ボタンは必ず用意し、OBSでクロップしやすい右上に小さく置く。
- ショートカットキーで編集モードへ戻れる場合でも、復帰ボタンは残す。

### 多言語デザイン

- 日本語と英語で文言長が変わっても、ボタン幅やパネル幅が破綻しないようにする。
- 固定幅ボタンに長文ラベルを詰め込まない。
- UI文言は短く、配信画面に映り込んでも邪魔にならない表現にする。

### レスポンシブ方針

- デスクトップでは盤面と編集パネルを横並びにできる。
- 幅が狭い場合は、盤面を上、編集パネルを下に積む。
- 表示モードでは常に盤面優先で、余白と盤面サイズを安定させる。
- OBS想定サイズの1920 x 1080と1280 x 720で、盤面が画面内に収まり、文字が読めることを優先する。

### 表示モード画面

編集モードOFF時は、OBSのウィンドウキャプチャに取り込む主画面として扱う。

- 盤面を主役にして中央に配置する。
- 操作パネル、入力欄、編集系ボタンは非表示にする。
- 背景はアプリで選んだ背景色またはテーマ背景を表示する。
- セルクリックによる項目開け、リーチ、ビンゴ表示は利用できる。
- 右上に小さな編集復帰ボタンを必ず置く。
- 補助操作としてショートカットキーでも編集モードへ戻れるようにしてよい。
- アドレスバーやブラウザ枠はOBS側のクロップで除外する前提にする。

### フッター

編集モードでは、画面下部に控えめなフッターを表示する。

- 著作権表記は `©UTAGE.GAMES` とし、リンク先は `https://utage.games/` とする。
- フィードバックリンクを置き、日本語では `バグ報告・機能要望`、英語では `Bug Reports & Feature Requests` と表示する。
- フィードバックリンクのURLは `https://github.com/utagestudio/bingo/issues` とする。
- 表示モードではOBS取り込みの邪魔にならないよう、フッターは非表示または極力控えめにする。

### overlay画面

- OBSブラウザソースなどで表示専用URLを使いたい場合の副運用として提供する。
- 盤面のみ表示する。
- 必要なら小さなスロット切り替えだけ残す。
- 背景透過を選べる。
- 項目開け、リーチ、ビンゴ表示はoverlayでも反映する。
- URLクエリで初期表示を制御する。
- LocalStorageはブラウザプロファイルやオリジン単位で分離されるため、通常ブラウザで編集した状態がOBSブラウザソース側へ共有されない場合がある。

候補:

```text
/?view=overlay
/?view=overlay&board=board-2
/?view=overlay&transparent=1
/?view=overlay&lang=en
```

### 空状態

項目がない場合:

- 1 x 1のFree Space盤面を表示する。
- 編集モードでは入力欄にフォーカスしやすくする。
- overlayではFree Spaceのみ表示し、ビンゴ状態として扱う。

## 非機能要件

### パフォーマンス

- 25-100セル程度で滑らかに動作する。
- LocalStorage保存はdebounceして過剰書き込みを避ける。
- 大きな依存ライブラリは避ける。

### アクセシビリティ

- ボタンは意味のあるラベルを持つ。
- 編集モード状態が視覚的に分かる。
- コントラストを十分に確保する。
- ドラッグ操作に依存しすぎないよう、将来的にキーボード操作を追加できる構造にする。

### OBS適性

- ウィンドウキャプチャ時に盤面が安定した位置へ表示される。
- 編集モードOFF時に操作UIが映り込まない。
- アプリ背景をそのまま配信背景として使える。
- OBS側でクロップしやすい余白設計にする。
- 解像度変更時に盤面が中央に収まる。
- フォントサイズが極端に小さくならない。
- アニメーションは控えめにする。

## エラー処理

- LocalStorageが使えない場合はメモリ上で動作し、保存不可であることをUIに表示する。
- 保存データが壊れている場合は初期状態に戻し、可能なら壊れたデータを退避キーへ保存する。
- 盤面生成中に不整合があれば `rawInput` を正として再生成する。

## テスト方針

### 単体テスト

- 項目数から盤面サイズを計算できる。
- Free Spaceが中央優先で配置される。
- Free SpaceがD&Dやシャッフル対象から除外される。
- 入力テキストから項目配列を作れる。
- ラベル変更時に既存IDが維持される。
- 行追加・行削除時に項目IDとレイアウトが仕様どおり更新される。
- shuffleが項目数を保つ。
- LocalStorage保存データを復元できる。
- 壊れた保存データを初期化できる。
- marked状態からリーチラインを判定できる。
- marked状態からビンゴラインを判定できる。
- セル状態の表示優先順位を `bingo > reach > marked > normal` として算出できる。
- 1 x 1盤面のFree Spaceをビンゴとして判定できる。
- localeの初期化、保存、URLクエリ上書きができる。
- light/darkテーマ設定を保存・復元できる。
- 表示サイズ設定を保存・復元できる。

### UIテスト

- 編集モードONでドラッグ可能。
- 編集モードOFFでドラッグ不可。
- 編集モードONではセルクリックで項目開けが発生しない。
- 表示モードでセルクリックによる項目開けができる。
- Free Spaceセルはクリックしても閉じない。
- Free Spaceセルはドラッグできない。
- 編集復帰ボタンで表示モードから編集モードへ戻れる。
- ランダムボタンで盤面順序が変わる。
- 3つの保存スロットを切り替えられる。
- リロード後に入力と盤面が復元される。
- overlay URLで編集UIが表示されない。
- overlay URLで項目開け、リーチ、ビンゴ表示が崩れない。
- 編集モードOFFの画面をOBSウィンドウキャプチャ想定でクロップしやすい。
- 日本語UIと英語UIを切り替えられる。
- light/darkテーマを切り替えられ、表示モードにも反映される。
- 表示サイズを切り替えられ、表示モードの盤面サイズに反映される。
- 選択状態を全解除すると、項目セルだけが未選択に戻り、Free Spaceは開いたままになる。
- 初期状態に戻すと、現在言語に対応した初期サンプルまたは空盤面へ戻る。
- フッターの著作権リンクとフィードバックリンクが、言語に応じた文言で表示される。

### 表示確認

- 1920 x 1080
- 1280 x 720
- 390 x 844
- OBSウィンドウキャプチャ相当の表示
- OBSブラウザソース副運用時のoverlay表示

## 初期実装スコープ

実装する:

- 3スロット保存
- 複数行入力
- 自動盤面サイズ
- 中央優先Free Space
- 項目開け、ハイライト表示
- リーチ表示
- ビンゴ表示
- ランダム並び替え
- 編集モード
- ドラッグアンドドロップ
- LocalStorage自動保存
- OBSウィンドウキャプチャ向け表示モード
- overlay表示: 副運用
- 多言語対応: 日本語、英語
- light/darkテーマ切り替え
- 表示モードのcompact/standard/fitサイズ切り替え
- Cloudflare向けビルド設定

実装しない:

- サーバー保存
- ユーザー認証
- 共有URL
- 画像セル

## 将来拡張

- 盤面をPNGとして書き出す。
- Cloudflare KVまたはD1へ保存して複数端末で共有する。
- OBS WebSocketと連携する。
- テーマプリセットを増やす。
- 共有用読み取り専用URLを発行する。
